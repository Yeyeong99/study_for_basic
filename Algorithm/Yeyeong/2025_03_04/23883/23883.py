import sys
sys.stdin = open("input.txt", "r")


def inorder(node, i):
    if not node[i]:
        for


T = int(input())

for t in range(1, T + 1):
    N = int(input())
    nodes = [[] for _ in range(N + 1)]

    for n in range(1, N):
        if